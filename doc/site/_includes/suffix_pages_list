{% for weight in (1..10) %}{% for node in suffix_pages_list %}
      {% if weight == node.weight and group == node.group %}
         {% if page.url == node.url %}
            <li class="nav-noindent"><a href="{{node.url}}">{{node.title}}</a></li>
         {% else %}
            <li class="nav-noindent"><a href="{{node.url}}">{{node.title}}</a></li>
         {% endif %}
      {% endif %}
{% endfor %}{% endfor %}
{% assign suffix_pages_list = nil %}
{% assign group = nil %}
package cc.factorie.debug

/**
A tool for debugging models and template
@author Michael Wick and Sameer Singh
@since 0.9
@see Template
*/
object TemplateChecker
{
  def checkTemplate(t:Template) : Boolean =
    {
      false
    }

  def diffCheck : Boolean =
    {
      //check if efficient calculation of diff is same as exhuastive calcuation of diff
    }
}
